<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>
{{#each posts as |post| }}
<div class="row mb-4 post" data-id="{{id}}">
  <div class="col-md-5">
    <h2>
    {{post.title}}
    </h2>
    <p>Created by {{post.user.name}} on {{format_date post.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.content}}
    </p>
  </div>
</div>
  <div class="col-md-12">
    <form class="form update-form" data-id="{{post.id}}" style="display: none;">
      <div class="form-group">
        <label for="update">Update post:</label>
        <textarea class="form-input" id="update-{{post.id}}" rows="3"></textarea>
      </div>
      <button type="button" class="btn btn-primary update-button" data-id="{{post.id}}">Update</button>
      <button type="button" class="btn btn-primary delete-button" data-id="{{post.id}}">Delete</button>
    </form>
  </div>
{{/each}}
<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Post:</h3>

    <form class="form new-post-form">
      <div class="form-group">
        <label for="post-title">Post Title:</label>
        <input class="form-input" type="text" id="post-title" name="post-title" />
      </div>
      <div class="form-group">
        <label for="post-content">Content:</label>
        <textarea class="form-input" id="post-content" name="post-content"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>

  {{!-- {{#if post.length}}
  <div class="col-md-6 post-list">
    <h3>Current Posts:</h3>

    {{#each posts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div> --}}

<script src="./js/post.js"></script>
<script src="./js/comment.js"></script>
